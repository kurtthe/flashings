# Getting Started

install the dependencies
```
yarn install
```

after install the dependencies continue with the pods 
```
yarn pod:install
```

## Start the App
```
# using run for ios
yarn ios:dev

# using run for ios
yarn android:dev s
```
